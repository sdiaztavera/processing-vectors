
Vector center;
Vector mouse;

void setup () {
  size(600, 600);
  
  center = new Vector(width * 0.5, height * 0.5);
  mouse = new Vector();
}

void draw () {
  background(255);
  
  // Calculamos el vector resta
  // mouse = mouse - center
}
